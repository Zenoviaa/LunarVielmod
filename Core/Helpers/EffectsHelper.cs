using Microsoft.Xna.Framework;
using Terraria;
using Terraria.ModLoader;

namespace Stellamod.Core.Helpers
{
    internal static class EffectsHelper
    {
        public static Projectile SimpleExplosionCircle(Projectile baseProjectile, Color explosionColor, float startRadius = 4, float endRadius = 64, float width = 24)
        {
            return baseProjectile;
        }
    }
}
